# Admin (required for /admin login)
ADMIN_PASSWORD=change-me-long-random
ADMIN_SESSION_SECRET=another-long-random-secret-for-signing-cookies

# Optional: live edits without git — create table `site_content` (id int, data jsonb, updated_at)
# See `supabase/migrations/001_site_content.sql`
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
